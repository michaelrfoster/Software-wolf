<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mrfoster.software_wolf">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".DeadActivity" />
        <activity android:name=".VillagerWinActivity" />
        <activity android:name=".BoringNightActivity" />
        <activity android:name=".WerewolfWinActivity" />
        <activity android:name=".NightActivity" />
        <activity android:name=".DayActivity" />
        <activity android:name=".RoleActivity" />
        <activity android:name=".LobbyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>